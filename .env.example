# DreamWeaver Backend Environment Configuration
# Copy this file to .env and update the values for your environment

# MongoDB Connection String
# Replace with your actual MongoDB connection string
# For local MongoDB: mongodb://localhost:27017/dreamweaver
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/dreamweaver
MONGODB_URI=mongodb://localhost:27017/dreamweaver

# JWT Secret Key for token signing
# Generate a secure random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production

# Server Port (optional, defaults to 3000)
PORT=3000

# Environment (development, production, test)
NODE_ENV=development

# Frontend URL for CORS (only needed in production)
# FRONTEND_URL=https://your-frontend-domain.com
